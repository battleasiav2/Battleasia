# Coolify — no bundled nginx (Coolify / Traefik owns SSL + domains)
#
# Domains:
#   player → https://battleasia.gg
#   shop   → https://shop.battleasia.gg
#   admin  → https://admin.battleasia.gg
#   api    → https://battleasia.gg/api  (and /api on shop + admin if needed)
#
# Required Coolify env: JWT_SECRET, ADMIN_PASSWORD

name: battleasia-coolify

services:
  mongo:
    image: mongo:7
    restart: unless-stopped
    command: ["--replSet", "rs0", "--bind_ip_all"]
    volumes:
      - battleasia_mongo:/data/db
    expose:
      - "27017"
    healthcheck:
      test:
        [
          "CMD",
          "mongosh",
          "--quiet",
          "--eval",
          "try { rs.status().ok } catch (e) { rs.initiate({_id:'rs0',members:[{_id:0,host:'mongo:27017'}]}).ok }",
        ]
      interval: 20s
      timeout: 10s
      retries: 10
      start_period: 20s

  api:
    build:
      context: ${API_PATH:-../../battleasianew/api}
      dockerfile: deploy/Dockerfile
    restart: unless-stopped
    expose:
      - "5050"
    environment:
      NODE_ENV: production
      PORT: 5050
      MONGODB_URI: ${MONGODB_URI:-mongodb://mongo:27017/battleasia?replicaSet=rs0}
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      ADMIN_EMAIL: ${ADMIN_EMAIL:-admin@battleasia.gg}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:?ADMIN_PASSWORD is required}
      ADMIN_USERNAME: ${ADMIN_USERNAME:-admin}
      CORS_ORIGINS: ${CORS_ORIGINS:-https://battleasia.gg,https://www.battleasia.gg,https://shop.battleasia.gg,https://admin.battleasia.gg}
      APP_URL: ${APP_URL:-https://battleasia.gg}
      CDN_URL: ${CDN_URL:-}
      COINGO_MOCK: "false"
      LOG_AUTH_CODES: "false"
      SMTP_HOST: ${SMTP_HOST:-}
      SMTP_PORT: ${SMTP_PORT:-465}
      SMTP_SECURE: ${SMTP_SECURE:-true}
      SMTP_USER: ${SMTP_USER:-}
      SMTP_PASS: ${SMTP_PASS:-}
      MAIL_FROM: ${MAIL_FROM:-}
      MAIL_FROM_NAME: ${MAIL_FROM_NAME:-BattleAsia}
      SENTRY_DSN: ${SENTRY_DSN:-}
      FCM_SERVER_KEY: ${FCM_SERVER_KEY:-}
      BACKUP_S3_ACCESS_KEY: ${BACKUP_S3_ACCESS_KEY:-}
      BACKUP_S3_SECRET_KEY: ${BACKUP_S3_SECRET_KEY:-}
      BACKUP_S3_BUCKET: ${BACKUP_S3_BUCKET:-}
      BACKUP_S3_REGION: ${BACKUP_S3_REGION:-}
      BACKUP_OFFSITE_CMD: ${BACKUP_OFFSITE_CMD:-}
    volumes:
      - api_uploads:/app/uploads
    depends_on:
      mongo:
        condition: service_healthy

  player:
    build:
      context: .
      dockerfile: docker/player.Dockerfile
      args:
        VITE_BAC_SHOP_URL: ${VITE_BAC_SHOP_URL:-https://shop.battleasia.gg}
        VITE_SENTRY_DSN: ${VITE_SENTRY_DSN:-}
    restart: unless-stopped
    expose:
      - "80"
    depends_on:
      api:
        condition: service_healthy

  shop:
    build:
      context: .
      dockerfile: docker/shop.Dockerfile
      args:
        VITE_MAIN_APP_URL: ${VITE_MAIN_APP_URL:-https://battleasia.gg}
        VITE_BAC_SHOP_URL: ${VITE_BAC_SHOP_URL:-https://shop.battleasia.gg}
        VITE_SENTRY_DSN: ${VITE_SENTRY_DSN:-}
    restart: unless-stopped
    expose:
      - "80"
    depends_on:
      api:
        condition: service_healthy

  admin:
    build:
      context: .
      dockerfile: docker/admin.Dockerfile
      args:
        VITE_PLAYER_URL: ${VITE_PLAYER_URL:-https://battleasia.gg}
        VITE_SENTRY_DSN: ${VITE_SENTRY_DSN:-}
    restart: unless-stopped
    expose:
      - "80"
    depends_on:
      api:
        condition: service_healthy

volumes:
  battleasia_mongo:
  api_uploads:
